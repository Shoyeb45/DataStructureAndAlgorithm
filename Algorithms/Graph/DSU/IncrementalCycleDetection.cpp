/*
 Problem Description:
 Processing a series of queries that add undirected edges to a graph and determining after each addition whether the graph contains a cycle.
 If there is cycle don't process them else add the edge in the graph.
*/ 

#include<bits/stdc++.h>
using namespace std;

int main() {
    int q;
    cin >> q;

    for (int i = 0, u, v; i < q; i++) {
        cin >> u >> v;
        
    }
}